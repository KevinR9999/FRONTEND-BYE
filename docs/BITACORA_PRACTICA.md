# Bitácora de Tareas de Practica Profesional

| N° de la act. | Descripción de la tarea realizada | Fecha de Ejecución | # de horas(*) | Firma |
|---------------|-----------------------------------|-------------------|---------------|-------|
| 9 | Implementación del sistema de logros y recompensas con librería Lucide React. Creación de tablas en Supabase (achievements, user_achievements), desarrollo de tipos TypeScript, servicio de logros y página de visualización. Definición de 23 logros en 7 categorías. | 05/02/2026 | 8 | |
| 10 | Implementación del sistema de racha (streak). Lógica para actualizar automáticamente los días consecutivos de práctica. Modificación de tabla profiles con campo last_activity_date. | 05/02/2026 | 3 | |
| 11 | Diseño y creación de 23 íconos personalizados para logros en Figma. Subida e integración de imágenes PNG al proyecto en public/achievements/. | 07/02/2026 | 4 | |
| 12 | Implementación de animación popup estilo TikTok al desbloquear logros con confeti (canvas-confetti). Optimización del rendimiento de la animación. | 07/02/2026 | 3 | |
| 13 | Implementación de indicadores de progreso en logros bloqueados con barras de color por categoría. Corrección de bug de desbloqueo: logros ahora solo se otorgan al aprobar lección (80%+). | 07/02/2026 | 3 | |
| 14 | Balanceo del sistema de XP: reducción de 10 a 5 XP por respuesta correcta. Ajuste de thresholds de logros XP en base de datos para mayor dificultad progresiva. | 07/02/2026 | 2 | |
| 15 | Rediseño de página de logros: íconos más grandes (72px), colores por categoría, candado en bloqueados, header azul, estructura visual mejorada. | 07/02/2026 | 3 | |
| 16 | Eliminación de restricción CHECK CONSTRAINT en columna `level` de la tabla `lessons` en Supabase para permitir niveles dinámicos (no solo A1-B2). Verificación de que las lecciones existentes no se vieron afectadas. | 08/02/2026 | 1 | |
| 17 | Sincronización automática de logros para usuarios existentes. Al abrir la página de logros se verifican y desbloquean automáticamente los logros que el usuario ya cumple, sin necesidad de intervención manual. | 09/02/2026 | 2 | |
| 18 | Actualización del panel de administración de lecciones: niveles dinámicos con pestañas (tabs), tarjetas clickeables para editar lecciones, soporte para crear niveles personalizados (ej. "AYUDA", "C1"). Mejora del ranking con columnas de racha y logros. | 09/02/2026 | 4 | |
| 19 | Corrección de bugs en el editor de ejercicios del admin: error de JSON.parse en opciones MCQ (columna jsonb de Supabase devuelve objetos ya parseados), input de XP que no permitía borrar el valor (uso de estado string interno), visualización de ejercicios match con formato de dos columnas (izquierda/derecha). | 09/02/2026 | 3 | |
| 20 | Corrección de ejercicios tipo word-order: palabras no aparecían tras recargar la página por race condition en useEffect de React (dependencias no cambiaban al recargar las mismas preguntas). Solución: delegar inicialización del pool de palabras al useEffect dedicado. | 10/02/2026 | 2 | |
| 21 | Implementación de eliminación de niveles (tabs) en el panel admin con diálogo de confirmación. Eliminación en cascada: primero las preguntas de las lecciones del nivel, luego las lecciones, y actualización del estado local. Icono AlertTriangle y modal de confirmación. | 10/02/2026 | 2 | |
| 22 | Implementación de sistema de prerequisitos por nivel. Nuevo campo `required_level` en tabla `lessons` (migración SQL en Supabase). Selector en admin para configurar qué nivel debe completarse antes de desbloquear otro (ej. "Para desbloquear AYUDA: completar A1"). Integración en página de estudiante para verificar desbloqueo tanto a nivel de nivel como de lección individual. | 10/02/2026 | 3 | |
| 23 | Configuración de XP por pregunta individual (`xp_reward`) en el editor de ejercicios del admin. Campo numérico editable con valor por defecto de 5 XP. La página del estudiante usa `q.xp_reward` para calcular el puntaje de cada respuesta correcta. | 10/02/2026 | 2 | |
| 24 | Corrección de formulario de preguntas diagnósticas en admin: error 400 Bad Request al crear preguntas. Se incluyeron todas las columnas de la tabla (`audio_url`, `image_url`, `time_limit`, `order_index`). Se añadió logging detallado de errores en `adminService.ts`. | 12/02/2026 | 2 | |
| 25 | Eliminación de restricción CHECK CONSTRAINT en columna `skill` de la tabla `diagnostic_questions` en Supabase. El constraint solo permitía (reading, listening, writing, speaking) pero se necesitaban también grammar y vocabulary. Ejecución de `ALTER TABLE diagnostic_questions DROP CONSTRAINT diagnostic_questions_skill_check`. | 12/02/2026 | 1 | |
| 26 | Implementación de ejercicio tipo word_order en formulario de diagnóstico: inputs individuales para cada palabra con botones de agregar/eliminar, soporte para palabras distractoras, sincronización automática de `audio_text` y `correct_answer` en ejercicios tipo speaking. | 12/02/2026 | 2 | |
| 27 | Corrección de avatares en página de usuarios del admin: reemplazo de letra única por foto de perfil (Google OAuth `avatar_url`) con fallback a iniciales estilo Google (dos letras, colores variados por usuario). Componente `UserAvatar` con manejo de error de carga de imagen. | 13/02/2026 | 2 | |
| 28 | Implementación de tracking de última conexión: llamada a `updateLastSeen()` en `authStore.ts` al verificar sesión activa. La función existía en `adminService.ts` pero nunca se invocaba. Ahora se ejecuta fire-and-forget cada vez que el usuario inicia sesión o recarga la app. | 13/02/2026 | 1 | |
| 29 | Corrección de desactivación de usuarios: el bloqueo no funcionaba para login con Google (OAuth). Se modificó `onAuthStateChange` en `App.tsx` para llamar `checkSession()` (que verifica `is_active`) en vez de solo `setAuthenticated(true)`. Se agregó `blockedMessage` al store de autenticación para mostrar mensaje en la página de login cuando una cuenta está deshabilitada. | 13/02/2026 | 2 | |
| 30 | Rediseño de esquema de colores del panel de administración: eliminación completa de colores violeta/fuchsia (asociados a IA) en los 10 archivos del admin. Nuevo esquema minimalista con tonos slate para elementos principales, azul para badges de rol, y colores variados por hash para avatares sin foto. | 13/02/2026 | 2 | |
| 31 | Implementación de notificaciones push: creación de Edge Function `send-notification-push` en Supabase que envía push a dispositivos mediante Web Push API con `@pushforge/builder`. Integración en panel admin (`NotificationsPage.tsx`) para enviar push al crear, editar o enviar notificaciones. Resolución de target_mode (todos, por nivel, usuarios específicos). | 17/02/2026 | 4 | |
| 32 | Implementación de banner de permisos push en Dashboard: detección automática de soporte Web Push y estado de permisos. Banner con botones "Activar" y "Ahora no" que aparece cuando el usuario no ha dado permiso. Persistencia del descarte en localStorage. Integración con `ensurePushSubscription()` del módulo `pushClient.ts`. | 17/02/2026 | 2 | |
| 33 | Rediseño del panel de notificaciones del Dashboard para móvil: implementación de ventana dropdown minimalista con fondo blanco, indicador dot para no leídas, animación slideUp, backdrop semitransparente, botón "Leer todas" y adaptación responsive. Ancho dinámico `calc(100vw-2rem)` en móvil. | 17/02/2026 | 3 | |
| 34 | Optimización de velocidad de entrega de notificaciones push: adición de header `Urgency: high` y `TTL: 60` en las peticiones Web Push para priorizar entrega inmediata en FCM (Google). Cambio de envío secuencial a paralelo con batches de 10 usando `Promise.allSettled`. | 17/02/2026 | 2 | |
| 35 | Implementación de panel de configuración funcional del admin (`SettingsPage.tsx`). Creación de tabla `app_settings` (key-value) en Supabase con políticas RLS. Servicio `appSettingsService.ts` con caché en memoria. Conexión de ajustes a páginas de estudiante: porcentaje mínimo para aprobar y preguntas por intento en lecciones, tiempo límite del diagnóstico. | 17/02/2026 | 3 | |
| 36 | Implementación de configuración avanzada de la prueba diagnóstica: distribución dinámica de preguntas por nivel (A1, A2, B1, B2) con tarjetas de colores editables. Input de total de preguntas con redistribución automática proporcional. Almacenamiento en `app_settings` con clave `diagnostic_questions_per_level`. | 17/02/2026 | 2 | |
| 37 | Implementación de distribución por tipo de ejercicio dentro de cada nivel en la prueba diagnóstica. Acordeón expandible por nivel con inputs para cada tipo (opción múltiple, completar, speaking, listening, ordenar palabras). El `diagnosticService.ts` usa la distribución manual del admin si existe, con fallback a distribución pareja automática. | 17/02/2026 | 3 | |
| 38 | Dinamización del modal de bienvenida (`DiagnosticModal.tsx`): los valores de preguntas totales y tiempo límite ahora se leen de `app_settings` en vez de estar hardcodeados. Corrección de visibilidad de texto en inputs del admin (faltaba `text-slate-900`). Corrección de función `updateAppSetting` que no guardaba por conflicto con política RLS de upsert. | 17/02/2026 | 1 | |
